name: 'compile peerconnection so'

on:
  push

jobs:
  compile:
    steps:
    - name: deploy
      runs-on: ubuntu-latest
      uses: appleboy/ssh-action@master
      with: 
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        script: |
          cm
          git pull
          compile_peerconnection.sh
